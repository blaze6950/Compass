<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zna.online.compass">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <user-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_compass"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_compass"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Authorization" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpEmailActivity" android:theme="@style/AppThemeWithActionBar" android:label="Sign up">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="zna.online.compass.Authorization" />
        </activity>
        <activity android:name=".SignInEmailActivity" android:theme="@style/AppThemeWithActionBar" android:label="Sign in">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="zna.online.compass.Authorization" />
        </activity>
    </application>

</manifest>